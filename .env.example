# Socket.io Server URL Configuration

# Development (local)
# VITE_SOCKET_URL=http://localhost:3001

# Production - Update with your actual server URL
VITE_SOCKET_URL=https://collab-whiteboard.phptutorialpoints.in

# Note: 
# - For local development, use http://localhost:3001
# - For production, use your deployed server URL (must match server CORS config)
# - The server must be running on the same domain or configured for CORS

